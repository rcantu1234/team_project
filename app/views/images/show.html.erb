<p id="notice"><%= notice %></p>

<p>
  <%= image_tag(@image.gram.url(:medium)) %>
</p>
<p>
  <strong>Caption:</strong>
  <%= @image.caption %>
</p>

<%= link_to 'Edit', edit_user_image_path(current_user, @image) %> |
<%= link_to 'Back', user_images_path %>
